node_modules
**/node_modules
dist
**/dist
.next
**/.next
coverage
.git
.env
.env.local
*.tsbuildinfo

# Nothing below is needed to build or run an image, and every one of them ends
# up in a layer if it reaches the context.
.git
.gitlab
docs
examples
test-results
playwright-report
.superpowers
.pnpm-store
*.md
!README.md

# The website (ADR-058). Its own dependencies and sources, none of which an image
# builds or runs; scripts/verify-image-contents.sh checks it stayed out.
site

# SBOMs from scripts/sbom.sh. The web image is built after the API image is
# attested in publish-images, and would otherwise copy them into its build stage.
sboms
