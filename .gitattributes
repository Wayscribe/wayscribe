# git archive writes the commit into BUILD_COMMIT, so a bundle built from an
# archive (Leadline's pin-sdk.sh, a GitLab source download) names the commit it
# came from in every event's runtime.sdk.commit (ADR-063).
packages/sdk-node/BUILD_COMMIT export-subst
