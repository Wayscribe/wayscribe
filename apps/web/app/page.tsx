export default function HomePage() {
  return (
    <main>
      <h1>Flight Recorder</h1>
      <p>
        Entity search and journey timelines arrive in Phase 2. This shell exists to confirm the
        application builds, starts, and is wired into the workspace.
      </p>
    </main>
  );
}
