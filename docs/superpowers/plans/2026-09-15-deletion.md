# Deletion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Each task is test-first. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Operators can remove captured data on demand: one journey, everything matching an identifier, or an environment's time window, each admin-only and audited without the erased value.

**Spec:** `docs/superpowers/specs/2026-09-15-deletion-design.md` (authoritative for behaviour).

**Depends on:** the key rotation branch being merged (erasure matches tokens under the keyring).

**Conventions for every task:** as in `docs/superpowers/plans/2026-09-15-key-rotation.md` (Node 24 PATH prefix; `pnpm format && pnpm lint && pnpm typecheck && pnpm test`, plus `pnpm test:integration` when the database is touched; explicit return types; no non-null assertions; comments explain why; no em dashes in docs prose; commit trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`). Branch `deletion`. Stage only your own files.

---

## Task 1: Repository functions

**Files:** `packages/database/src/repositories/deletion.ts` and `deletion.integration.test.ts` (create), `packages/database/src/index.ts`, `packages/database/src/repositories/retention.ts` (export or share the advisory lock key; do not change sweep behaviour).

Build `deleteJourney`, `findJourneysByIdentifier`, `eraseIdentifier`, `deleteRange`, `deleteReplayDestination` per the spec, each writing its audit row in the same transaction as the delete, erasure in batches of 500, range in batches of 1,000 under retention's advisory lock.

Prove: every "Integration, database" item in the spec's testing section.

## Task 2: API routes

**Files:** `apps/api/src/routes/deletions.ts` and `deletions.integration.test.ts` (create), `apps/api/src/app.ts` (or wherever routes register), a shared admin-authentication helper extracted from `apps/api/src/routes/replays.ts` if the replay routes currently inline it.

Build `DELETE /v1/journeys/:journeyId`, `POST /v1/erasures`, and `DELETE /v1/replay-destinations/:destinationId` per the spec, with request validation following the existing routes, the usual error envelope, and admin authentication shared with the replay routes rather than copied.

Prove: every "Integration, API" item in the spec.

## Task 3: CLI commands

**Files:** `packages/database/src/cli.ts`, a CLI integration test following any existing CLI test pattern (or repository-level tests plus a smoke run of each command against a local database documented in the report).

Build `delete:journey`, `delete:identifier`, `delete:range`, `delete:destination` per the spec, with readable dry-run tables and non-zero exits for a held lock, an unknown project or environment, and a malformed date.

Prove: each command's happy path and each non-zero exit, run for real against a local stack, with output pasted into the report.

## Task 4: Web confirmation flow

**Files:** `apps/web/app/(authenticated)/journeys/[journeyId]/delete/page.tsx` (create), `apps/web/app/api/journeys/[journeyId]/delete/route.ts` (create) and its `route.test.ts`, the journey page (the link), `apps/web/src/lib/api.ts` (a server-side `deleteJourney` call), the search page (the one-line notice from a query parameter), `apps/web/e2e/journey.spec.ts` or a new spec file (the browser test), `globals.css` only if needed.

Prove: the spec's "Web" and "Browser" items; the existing Playwright specs still pass.

## Task 5: Documentation and debt

**Files:** `docs/OPERATIONS.md`, `docs/API_SPEC.md`, `docs/SECURITY.md`, `docs/DECISIONS.md`, `README.md` (ADR count), `CHANGELOG.md`, `docs/LOCAL_DEVELOPMENT.md` CLI table, `packages/database/src/repositories/retention.ts` (remove `DEBT-N11B8K`).

Prove: docs-truth test passes; `npx debtwatch list` no longer lists `DEBT-N11B8K`; the OPERATIONS section's commands are the ones Task 3 ran.
